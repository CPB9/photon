#ifndef __PHOTON_CORE_UTIL_H__
#define __PHOTON_CORE_UTIL_H__

#include "photongen/onboard/Config.h"

#define PHOTON_MIN(a, b) (((a) > (b)) ? (b) : (a))
#define PHOTON_MAX(a, b) (((a) > (b)) ? (a) : (b))

#endif

